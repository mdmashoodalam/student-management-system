
# Application Configuration
spring.application.name=Student Management System

# Server Configuration (Railway automatically PORT set karta hai)
server.port=${PORT:8080}

# MySQL Database Configuration - Railway Variables
spring.datasource.url=jdbc:mysql://${MYSQLHOST:localhost}:${MYSQLPORT:3306}/${MYSQLDATABASE:student_management}
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:HABZxSBXGcoBCRzFeiANQVZzkhGLPOmj}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Error Handling
server.error.include-message=always
server.error.include-binding-errors=always
